# 工具重新编辑功能使用指南

## 功能概述

此功能允许用户编辑被审核拒绝的工具提交，每个工具最多可修改 **3 次**。

---

## 完整用户流程

### 1. 提交工具
- 访问 `/submit` 页面
- 填写工具信息（**建议填写邮箱**）
- 提交后看到成功模态框

### 2. 管理员审核
- 管理员登录：`/admin/login`（默认账号：`admin` / `admin123`）
- 访问审核页面：`/admin/review`
- 查看待审核工具详情
- **拒绝**工具并填写拒绝原因（必填）

### 3. 查看提交记录
**登录后直接查看**（无需输入邮箱）：

- 点击顶部导航栏的用户头像
- 在下拉菜单中点击"📁 我的提交"
- 自动加载您的所有提交记录

或者：
- 提交成功后点击"查看我的提交记录 →"链接
- 直接跳转到提交记录页面

### 4. 重新编辑工具
- 在"我的提交"页面查看工具列表
- 找到**审核不通过**的工具（红色徽章）
- 查看拒绝原因
- 点击"重新编辑"按钮（仅当 `edit_count < 3` 时显示）
- 跳转到 `/edit-tool/{id}` 页面

### 5. 编辑并重新提交
- 修改工具信息（预填充原有数据）
- 点击"更新工具"按钮
- 看到成功提示："更新成功！将重新进入审核流程"
- 自动跳转回"我的提交"页面

### 6. 再次审核
- 工具状态自动重置为**待审核**（`review_status=0`）
- 拒绝原因清空
- 修改次数 `edit_count` 增加 1
- 管理员可重新审核

---

## 重要说明

### 修改次数限制
- 每个工具最多可修改 **3 次**
- 达到 3 次后，"重新编辑"按钮消失
- 显示提示："已达修改上限"
- 如需进一步修改，联系管理员

### 编辑权限
- **只能编辑**审核不通过的工具（`review_status=1`）
- 待审核或已通过的工具无法编辑
- 后端会验证编辑权限，返回 403 错误

### 审核状态流转
```
提交 → 待审核(0) → 管理员审核
         ↓
    审核通过(2) / 审核不通过(1)
                     ↓
              用户编辑（edit_count++）
                     ↓
                 待审核(0)
                     ↓
               管理员再次审核
```

---

## 技术实现细节

### 数据库字段
- `review_status`: INTEGER (0=待审核, 1=不通过, 2=通过)
- `rejection_reason`: TEXT (拒绝原因，必填)
- `submitter_email`: VARCHAR (提交者邮箱，可选)
- `edit_count`: INTEGER (修改次数，默认 0)

### API 端点
- `GET /tools/my-submissions?email={email}` - 查询用户提交
- `PATCH /tools/{id}` - 更新工具（限制：review_status=1, edit_count<3）

### 前端页面
- `/my-submissions` - 我的提交列表
- `/edit-tool/{id}` - 编辑工具页面

### 安全验证
- 后端验证审核状态（只能编辑 review_status=1）
- 后端验证修改次数（edit_count < 3）
- 编辑后自动重置状态和拒绝原因

---

## 测试场景

### 场景 1：完整编辑流程
1. 登录后提交工具
2. 管理员拒绝（填写原因："Logo 不清晰"）
3. 用户点击头像 → "我的提交"
4. 看到拒绝原因，点击"重新编辑"
5. 上传新 Logo，提交
6. 确认状态重置为待审核，edit_count=1

### 场景 2：达到修改上限
1. 用户编辑工具第 1 次（edit_count=1）
2. 管理员再次拒绝
3. 用户编辑第 2 次（edit_count=2）
4. 管理员再次拒绝
5. 用户编辑第 3 次（edit_count=3）
6. 管理员再次拒绝
7. **"重新编辑"按钮消失**，显示"已达修改上限"

### 场景 3：查看提交记录
1. 用户登录后提交工具
2. 点击头像 → "我的提交"
3. 自动加载所有提交记录
4. 查看审核状态、拒绝原因、修改次数

---

## 未来优化建议

1. **邮件通知**
   - 审核结果通知（使用 submitter_email）
   - 达到修改上限提醒

2. **用户系统**
   - 登录后自动关联提交记录
   - 无需邮箱查询

3. **修改历史**
   - 记录每次编辑的内容
   - 管理员可查看修改历史

4. **批量操作**
   - 管理员批量审核
   - 用户批量查看所有提交

---

## 常见问题

**Q: 如何查看我的提交记录？**
A: 登录后点击头像，选择"我的提交"即可。

**Q: 未登录能查看提交记录吗？**
A: 不能。必须登录后才能查看和编辑提交。

**Q: 达到 3 次修改上限后怎么办？**
A: 联系管理员手动重置 edit_count 或审核通过。

**Q: 已通过审核的工具能编辑吗？**
A: 不能。只能编辑审核不通过的工具。如需修改已通过的工具，联系管理员。

**Q: 编辑后多久会重新审核？**
A: 编辑后立即进入待审核状态，管理员可随时审核。

---

## 联系方式

如有问题，请联系管理员：admin@ai-tool-hub.com
